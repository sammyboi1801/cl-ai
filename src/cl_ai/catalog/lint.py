"""Consistency rules. Fails a tool rather than emitting a weak schema: no opaque
`args` param, every param described, enums where the value set is closed, name
convention, at least one golden example.
"""
