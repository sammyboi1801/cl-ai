"""Deliberately non-conforming backend: invalid arguments, missing confidence,
prose instead of refusal, ignores max_tools.

Exists because an abstraction with one implementation is not an abstraction. If
core survives this, it survives a hosted model.
"""
