"""PowerShell. OBJECT pipeline -- not a syntactic variant of POSIX.

A text-oriented plan rendered naively here runs and returns wrong results, which
is worse than an error. Uses the IR stage types to pick the idiomatic form, and
refuses when no faithful translation exists.
"""
